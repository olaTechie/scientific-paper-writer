{
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "test -f PAPER_CONTEXT.md && echo 'PAPER_CONTEXT.md detected — use study details (study type, primary outcome, target journal, citation style, output format, folder structure, integration preferences, figure generation preferences) to pre-load context for writing commands.' || echo 'OK'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
